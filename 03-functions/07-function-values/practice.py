"""Practice: use functions as data."""

# Level 1 - Define triple(number) and pass it to apply_once(function, value).

# Level 2 - Use sorted(..., key=...) to sort words by their last character.
words = ["alpha", "echo", "bravo", "delta"]

# Level 3 - Use filter() to keep scores of at least 70.
scores = [55, 72, 91, 64, 88]

# Level 4 - Write transform(values, function) with a loop, then use it to
# convert temperatures from Celsius to Fahrenheit.