"""Capstone: build a reusable transaction report."""

# Implement these as separate functions:
# 1. valid_transaction(record): amount is non-negative and category is present.
# 2. clean_transactions(records): return only valid records.
# 3. total_by_category(records): return a dictionary of category totals.
# 4. format_report(totals): return readable lines sorted by category.
# Challenge: add largest_transaction(records), handle an empty input, and use
# one higher-order helper or comprehension with a clear justification.