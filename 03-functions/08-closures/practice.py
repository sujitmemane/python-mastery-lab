"""Practice: build a closure for reusable configuration."""

# Level 1 - Complete make_prefix(prefix) so the returned function adds it.

# Level 2 - Create add_warning and use it on a message.

# Level 3 - Create make_counter() whose returned function increments a count.

# Level 4 - Explain why each call to make_counter() needs its own count.