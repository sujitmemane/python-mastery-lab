# Hints

1. Define an inner function that returns `prefix + message`.
2. Return the inner function, then call the result.
3. Use `nonlocal count` before changing the enclosing variable.
4. Each invocation creates a new enclosing scope and a new captured value.