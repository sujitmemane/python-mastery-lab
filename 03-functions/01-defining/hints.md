# Hints

1. Start with `def greet():` and indent the print statement.
2. The repetition belongs inside the function body.
3. Put one `print()` call per choice.
4. Define `start()` and call `show_menu()` inside it.