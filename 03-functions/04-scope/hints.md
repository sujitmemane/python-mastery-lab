# Hints

1. A function can read a global name, but a local assignment creates a local name.
2. Use `return score` and print the call.
3. The two names are in different scopes.
4. Define `show_score(value)` and use `value` inside it.