# Hints

1. Use `return number * number`.
2. Compare the two values with `if`.
3. An even number has remainder zero after division by 2.
4. Loop through the list and add one when `is_even(number)` is true.