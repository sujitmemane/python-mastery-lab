# Hints

1. Return `number * 2`.
2. The decorator returns a wrapper that calls `function()`.
3. Call `function(*args, **kwargs)`.
4. Store the result, inspect it, then return it. A decorator should preserve the wrapped function's contract.