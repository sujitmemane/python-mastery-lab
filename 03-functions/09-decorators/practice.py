"""Practice: write a decorator that validates a function's result."""

# Level 1 - Write double(number) and call it normally.

# Level 2 - Write show_result(function) that prints the function's result.

# Level 3 - Preserve arguments by using *args and **kwargs in the wrapper.

# Level 4 - Decorate a function that returns a number and print "valid" only
# when its result is non-negative. Return the original result either way.