# Hints

1. The first character uses index `0`; the last uses index `-1`.
2. Use `len(word) // 2` for the middle index.
3. Use `range(len(word))` to produce indexes.
4. Store `word[0]`, then compare every character to it.
