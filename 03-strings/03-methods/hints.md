# Hints

1. Call `.strip()` before `.lower()`.
2. `replace(old, new)` changes every matching occurrence by default.
3. Use `.removeprefix("ID:")` and `.removesuffix(".txt")`.
4. Use `.strip().split()` to discard repeated whitespace, then use `" ".join(...)`.
