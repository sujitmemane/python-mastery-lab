# Hints

1. Start the string with `f` and put variables inside `{}`.
2. Multiply `price * quantity` and use `:.2f` for two decimals.
3. Use the format specifier `:.1%`.
4. The total is `quantity * unit_price`.
