# Hints

1. `.lower()` returns a new string; keep the original variable unchanged.
2. Loop through `text.lower()` and check `character in "aeiou"`.
3. Use `len(sentence.split())`.
4. Count characters with `len(text)`, words with `len(text.split())`, and vowels with a loop.
