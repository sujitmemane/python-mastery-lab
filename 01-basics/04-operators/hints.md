# Hints

1. `//` gives the whole-number quotient and `%` gives the remainder.
2. Chain comparisons: `10 <= number <= 20`.
3. Make two equal list literals, then compare them with both operators.
4. Membership uses `in` and is case-sensitive.
5. See `solution.py` only after a genuine attempt.