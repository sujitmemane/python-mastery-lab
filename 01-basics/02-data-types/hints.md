# Hints

1. Literal syntax determines the initial type.
2. `type(value).__name__` gives a readable type name.
3. `isinstance(value, expected_type)` returns a boolean.
4. Check `10` separately against `int` and `str`.
5. See `solution.py` after attempting all three levels.