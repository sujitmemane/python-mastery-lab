# Hints

1. Call `int`, `float`, or `str` around the original value.
2. Invalid numeric text raises `ValueError`.
3. Put only the risky conversion inside `try` and return the converted value.
4. In `except ValueError`, return `None`.
5. Compare with `solution.py` after your attempt.