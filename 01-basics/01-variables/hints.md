# Hints

Use one hint, try again, and only then read the next one.

1. Assignment uses `=` and printing uses `print(...)`.
2. Python can assign `right, left = left, right`.
3. Tuple unpacking needs the same number of target names as values.
4. `alias.append(3)` changes the list that both names refer to.
5. Full solutions are in `solution.py`; explain the shared-list behavior in your own words.