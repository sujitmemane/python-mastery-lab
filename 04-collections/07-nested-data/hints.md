# Hints

1. Access the `"name"` key inside a loop.
2. Use `if "admin" in user["roles"]`.
3. Use a nested loop and accumulate by `item["product"]`.
4. A set would lose quantities; a dictionary maps each product to its total.