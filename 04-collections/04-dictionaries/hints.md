# Hints

1. Use string keys such as `{"title": "...", "year": 2020}`.
2. Use `get("director", "Unknown")` for a default.
3. Use `.items()` in a `for` loop.
4. Use `counts[word] = counts.get(word, 0) + 1`.