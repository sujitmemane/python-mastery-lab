# Hints

1. Both names refer to the same list object.
2. Use `items.copy()` before changing the new list.
3. A shallow copy shares the inner lists.
4. Use `copy.deepcopy()` when nested mutable values must be independent.