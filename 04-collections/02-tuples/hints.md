# Hints

1. A one-item tuple needs a trailing comma; three items do not.
2. Assign the tuple to three names in one statement.
3. Use `left, right = right, left`.
4. Track both values, then return `(smallest, largest)`.