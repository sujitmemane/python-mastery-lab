# Hints

1. Use square brackets and comma-separated values.
2. Use `append()` and `remove()`.
3. Use `enumerate()` or indexes from `range(len(items))`.
4. Start with the first number and replace the current largest when needed.