# Hints

1. Put the most specific or highest threshold first when ranges overlap.
2. Use `elif` for another condition and `else` for the fallback.
3. A score is valid only when `0 <= score <= 100`.
4. Check invalid input before checking whether it passes.
5. Compare your completed functions with `solution.py` after trying.