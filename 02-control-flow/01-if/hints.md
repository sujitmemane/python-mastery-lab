# Hints

1. An `if` statement needs a boolean condition and a colon.
2. Code controlled by `if` must be indented consistently.
3. Combine conditions with `and`.
4. The borrowing condition is `has_library_card and not has_overdue_books`.
5. The debug example is missing `:` after the comparison.