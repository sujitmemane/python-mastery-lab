# Hints

1. Print the number, then use `break` when `number == 7`.
2. Use `continue` when `number % 2 == 0`.
3. Print the number and `break` when both divisibility checks are true.
4. Use `if name == "": continue` before printing.
5. Compare with `solution.py` only after attempting every exercise.
