# Hints

1. Write `for item in collection:` and indent the action below it.
2. Use `len(word)` for the length of each word.
3. Create `total = 0`, then add each number to it.
4. Test each letter with `letter.lower() in "aeiou"`.
5. Compare with `solution.py` only after attempting every exercise.