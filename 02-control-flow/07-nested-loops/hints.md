# Hints

1. Print `*` inside the inner loop and print a blank line after it finishes.
2. The inner loop should run from 1 through the current `row`.
3. Print `first * second` inside the inner loop.
4. Compare `values[first_index]` with `values[second_index]` and increment the counter when equal.
5. Compare with `solution.py` only after attempting every exercise.
