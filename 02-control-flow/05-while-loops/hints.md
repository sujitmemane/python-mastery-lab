# Hints

1. Print `number`, then increase it by 1 inside the loop.
2. Print `countdown`, then decrease it by 1 each time.
3. Add `value` to `total`, then increase `value`.
4. Multiply `value` by 2 inside the loop.
5. Compare with `solution.py` only after attempting every exercise.
